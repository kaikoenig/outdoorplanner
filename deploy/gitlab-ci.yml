pages:
  stage: deploy
  script:
    - echo "Publishing pre-built site (built by GitHub Actions, pushed as-is)"
  artifacts:
    paths:
      - public
  rules:
    - if: '$CI_COMMIT_BRANCH == "main"'
